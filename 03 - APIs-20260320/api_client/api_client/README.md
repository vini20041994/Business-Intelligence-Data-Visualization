Implementação simples de um cliente de API, usado para consumir a API de alunos incluída neste repositório.

# Instalação das Bibliotecas

`pip install requests` ou `conda install requests` 

`pip install pydantic` ou `conda install pydantic`


# Execução

python client.py